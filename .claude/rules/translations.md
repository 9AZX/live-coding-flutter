# Translations

Look into `*.arb` files for desired translations; hint if a translation is missing. DO NOT update those files manually — instead run `flutter pub global run melos run l10n` (or `mise run l10n`) to get new wordings.