name: app_providers
description: Composition — agrège les bindProviders() de toutes les couches en une liste d'overrides.
version: 0.0.1

environment:
  flutter: ">=3.27.0"
  sdk: ">=3.11.0 <4.0.0"
resolution: workspace

dependencies:
  app_router:
  favorites_data:
  favorites_presentation:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  live_presentation:
  match_detail_presentation:
  matchs_data:
  matchs_presentation:
  network_dio_data:
  riverpod_annotation: ^4.0.2
  scores_domain:
  tactics_components:
  tactics_providers:

dev_dependencies:
  flutter_test:
    sdk: flutter
  givn:
  shouldly: ^0.5.0+1
