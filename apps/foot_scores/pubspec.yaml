name: foot_scores
description: App de démonstration — feed de scores foot (exercice de live coding).
version: 1.0.0+1

environment:
  flutter: ">=3.27.0"
  sdk: ">=3.11.0 <4.0.0"
resolution: workspace

dependencies:
  app_providers:
  app_router:
  # auto_route utilise les transitions Cupertino sur iOS, qui référencent ses glyphes.
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  scores_domain:
  tactics_providers:

dev_dependencies:
  flutter_test:
    sdk: flutter
  givn:
  shouldly: ^0.5.0+1

flutter:
  uses-material-design: true
