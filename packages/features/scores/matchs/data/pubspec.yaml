name: matchs_data
description: Couche data de la feature Matchs (impl TheSportsDB de ScoresRepository).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

resolution: workspace

dependencies:
  dio: ^5.7.0
  riverpod: ^3.2.1
  riverpod_annotation: ^4.0.2
  scores_domain:
