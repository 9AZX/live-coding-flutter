name: composition_root
description: Composition — agrège overrides (data, onglets, widget factories) + shell.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

resolution: workspace

dependencies:
  favorites_data:
  favorites_presentation:
  live_presentation:
  matchs_data:
  matchs_presentation:
  riverpod_annotation: ^4.0.2
  scores_shell:
