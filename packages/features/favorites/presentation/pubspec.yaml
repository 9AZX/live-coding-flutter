name: favorites_presentation
description: Feature Favoris (liste des matchs suivis).
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.10.0-0 <4.0.0"

resolution: workspace

dependencies:
  favorites_domain:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  scores_widgets:

flutter:
  uses-material-design: true
